# LA VAGUE Backend Environment Variables

# Server
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:3000

# Paystack (Get from https://dashboard.paystack.com)
PAYSTACK_SECRET_KEY=sk_test_your_secret_key_here
PAYSTACK_PUBLIC_KEY=pk_test_your_public_key_here

# ==========================================
# EMAIL CONFIGURATION
# ==========================================

# Email Provider: 'smtp' (default), 'gmail', or 'sendgrid'
EMAIL_PROVIDER=smtp

# SMTP Settings (for generic SMTP or Gmail)
# For Gmail: Use an App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# SendGrid Settings (alternative to SMTP)
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# Default "From" address (optional, defaults to SMTP_USER)
EMAIL_FROM=noreply@la-vague.store

# Email Test Mode (set to 'true' to log emails instead of sending)
EMAIL_TEST_MODE=false

# ==========================================
# ADMIN CONFIGURATION
# ==========================================

# Admin (Change these for production!)
# ADMIN_PASSWORD: Used for admin panel login
ADMIN_PASSWORD=your-secure-admin-password-here

# ==========================================
# DATABASE CONFIGURATION
# ==========================================

# Database (SQLite - no config needed for local development)
# Database file: database.sqlite

# PostgreSQL (for production - e.g., Render.com)
# DATABASE_URL=postgresql://user:password@host:port/database

# ==========================================
# ERROR TRACKING (Sentry)
# ==========================================

# Sentry DSN for error tracking (Get from https://sentry.io)
# Backend errors (Node.js)
SENTRY_DSN=https://your-sentry-dsn-here@o0.ingest.sentry.io/0

# Frontend DSN (can be same as backend or separate project)
# SENTRY_FRONTEND_DSN=https://your-frontend-dsn@o0.ingest.sentry.io/0

# Release version (optional, defaults to 1.1.0)
# RELEASE=1.1.0

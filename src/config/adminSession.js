// Bump this value whenever all previously issued admin sessions must be invalidated.
// This is not a secret; it is a schema/session compatibility version.
export const ADMIN_SESSION_GENERATION = 1;
